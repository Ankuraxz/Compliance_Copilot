# Use legacy peer deps to resolve HeroUI v3 and Tailwind CSS v3 compatibility
# HeroUI v3 requires Tailwind CSS v4, but we're using Tailwind v3 for compatibility
# with other dependencies like @tailwindcss/typography
legacy-peer-deps=true

